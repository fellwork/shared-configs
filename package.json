{
  "name": "shared-configs",
  "description": "Monorepo for shared development configs",
  "packageManager": "pnpm@6.32.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "npx eslint . --ext .js,.ts,.json,.yml,.yaml --fix",
    "test": "turbo run test"
  },
  "dependencies": {
    "esbuild": "^0.14.25",
    "esbuild-runner": "^2.2.1",
    "turbo": "latest",
    "ultra-runner": "^3.10.5"
  },
  "devDependencies": {
    "@changesets/cli": "^2.21.1"
  }
}
