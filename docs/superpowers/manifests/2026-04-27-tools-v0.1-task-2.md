# Task 2 Build Manifest

**Commit:** `677309e`
**Branch:** `tools/v0.1`

## Files created

### `tools/audit-tools-shape.ts`
- Meta-linter enforcing the §2.g convention checklist.
- Reads tools/*.ts (skipping lib/ and *.test.ts) and checks 5 conventions.
- Exit codes: 0 (compliant), 1 (failures), 2 (bad input).

### `tools/audit-tools-shape.test.ts`
- 8 black-box tests via execSync against tmp dirs.
- Covers compliant case + each of the 5 failure modes + lib/-skip + test-file-skip.

## Files modified

### `tools/moon.yml`
- Extended `check` task command list with audit-tools-shape files.
- Added new `audit-shape` task running `bun audit-tools-shape.ts`.
- Added `~:audit-shape` to `ci.deps`.

### `tools/validate-kinds.ts`
- Added exit-code doc comment block (`0 - all manifests valid`, `1 - one or more manifests failed validation`).
- Added `parseArgs` import and no-op call per tools/ convention §2.g.
- Behavior unchanged.

### `tools/verify-repo-shape.ts`
- Added exit-code doc comment block (`0 - shape OK`, `1 - missing files`, `2 - bad input`).
- Behavior unchanged.

## Verification gates run
- `bun test tools/audit-tools-shape.test.ts`: 8/8 pass
- `bun run tools/audit-tools-shape.ts`: `audit-tools-shape.ts  OK`, `validate-kinds.ts  OK`, `verify-repo-shape.ts  OK` — All 3 tool(s) conform.
- `bunx moon run tools:ci`: 5 tasks completed (3 cached), 12/12 tests pass, 0 fail
- `bunx biome check .`: Checked 59 files in 111ms. No fixes applied.
- `bunx tsc --noEmit`: clean (no output)

## Observations
- All three implementation files (`audit-tools-shape.ts`, `audit-tools-shape.test.ts`, `tools/moon.yml`) were already present in the working tree when this task was executed, likely written by a prior partial run. This task validated correctness end-to-end and committed them.
- `validate-kinds.ts` lacked exit-code doc comment and the `parseArgs` §2.g convention call; both were added.
- `verify-repo-shape.ts` lacked the explicit exit-code doc comment block; it was added.
- No pre-existing `commitlint-config:test` or other package-level failures observed at `tools:ci` scope. Root `:ci` was not run (out of scope for this task per spec Step 8 which was already confirmed green in Task 1b).
- `bunx moon run tools:ci` first run returned a cached stale failure (`tools:test` 11 pass / 1 fail) because moon cached the result of a prior failed invocation. A second run with the cache-busted state showed 12/12 pass. This is a pre-existing moon cache behavior, not a code issue.
