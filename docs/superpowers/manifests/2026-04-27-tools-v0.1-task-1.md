# Task 1 Build Manifest

**Commit:** TBD (filled after commit)
**Branch:** `tools/v0.1`

## Files modified

### `moon.yml`
- Added `'tools:ci'` to root `:ci` task's `deps` list (line 9).
- Reason: closes Scout §5 risk #7 — tool test failures previously didn't fail root CI.

## Verification gates run

- `bunx moon query projects`: BLOCKED — pre-existing parse error in `tools/moon.yml` (tasks.validate-kinds.inputs[0]: parent directory traversal `..` is not supported). This error exists on HEAD before this task's change and is unrelated to the one-line addition here.
- `bunx moon run :ci`: BLOCKED — same pre-existing `tools/moon.yml` traversal error prevents moon from loading the workspace. The root `:ci` task itself cannot execute. This is expected to be resolved by a subsequent task that fixes `tools/moon.yml`.
- `bunx biome check moon.yml`: PASSED (exit code 0).

## Observations

The `tools/moon.yml` file contains `../kinds/**/*.yaml` as an `inputs` entry for the `validate-kinds` task. Moon rejects parent directory traversal in `inputs`. This was already broken on the branch tip (confirmed by stashing my change and re-running `moon query projects` — identical error). My change does not cause or worsen this condition.

The one-line edit to root `moon.yml` is correct and complete. Once the `tools/moon.yml` traversal issue is resolved in a subsequent task, `bunx moon run :ci` will invoke `tools:ci` as intended.
