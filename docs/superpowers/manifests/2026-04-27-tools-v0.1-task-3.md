# Task 3 Build Manifest

**Commit:** `b274fae6eaf36b24626c0fdb411c578bfbed990e`
**Branch:** `tools/v0.1`

## Files created

### `tools/lib/discover-packages.ts`
- Pure library module: exports `discoverPackages(repoRoot: string): DiscoveredPackage[]`.
- Reads root package.json's `workspaces` field (array or `{ packages: [...] }` form).
- Expands `<dir>/*` globs and bare-dir entries; skips invalid/private packages.
- No shebang, no top-level process.exit, safe to import.

### `tools/lib/discover-packages.test.ts`
- 9 white-box tests (imports the function directly).
- Covers empty workspaces, flat array form, nested form, private skip, missing-name skip,
  missing-root throw, malformed-JSON throw, deterministic order, isPrivate field.

## Files modified

### `tools/moon.yml`
- `check` task command extended to include `lib/discover-packages.ts` and `lib/discover-packages.test.ts`.
- `check` and `test` tasks' `inputs` lists extended with `'lib/**/*.ts'` glob.

### `tools/tsconfig.json`
- Added `"allowImportingTsExtensions": true` to support direct `.ts` imports in white-box tests.

### `tsconfig.json` (root)
- Added `"moduleResolution": "Bundler"`, `"module": "ESNext"`, and `"allowImportingTsExtensions": true`
  so that `bunx tsc --noEmit` (which uses the root tsconfig, including `tools/**/*.ts`) does not
  error on the `.ts` extension in the test's import statement.

## Verification gates run
- `bun test tools/lib/discover-packages.test.ts`: 9/9 pass
- Sanity-check against shared-configs workspace: `["@fellwork/biome-config", "@fellwork/commitlint-config", "@fellwork/cspell-config", "@fellwork/lefthook-config", "@fellwork/markdownlint-config", "@fellwork/oxlint-config", "@fellwork/release-config", "@fellwork/renovate-config", "@fellwork/tsconfig"]` — 9 `@fellwork/*` names as expected
- `bunx moon run tools:ci`: 5 tasks completed, 21/21 tests pass
- `bun run tools/audit-tools-shape.ts`: only 3 tools listed (`audit-tools-shape.ts`, `validate-kinds.ts`, `verify-repo-shape.ts`); `lib/discover-packages.ts` is NOT listed — lib/ skip confirmed
- `bunx biome check .`: Checked 61 files, no fixes applied, no errors
- `bunx tsc --noEmit`: clean (no output)

## Observations
- Biome required import specifiers in alphabetical order: `readFileSync` came after `readdirSync` in the original spec code. Fixed by reordering to `existsSync, readdirSync, readFileSync, statSync`.
- TypeScript strict mode (`exactOptionalPropertyTypes`, control-flow narrowing) flagged `manifest.private === true` after an earlier `continue` guard that already filtered `true` values — TypeScript correctly narrowed the type to `false | undefined`, making the comparison unreachable. Fixed by using the literal `false` for `isPrivate`, which is semantically correct (we never push private packages).
- `allowImportingTsExtensions` was not previously set in either tsconfig. The spec requires white-box tests to import `'./discover-packages.ts'` directly (with `.ts` extension), which is a Bun-native import style. Added to both `tools/tsconfig.json` and the root `tsconfig.json` to keep `bunx tsc --noEmit` clean.
- Pre-existing: `commitlint-config` package has a test failure unrelated to this task (noted from prior manifests). Not fixed here.
